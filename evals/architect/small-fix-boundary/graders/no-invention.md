---
type: llm
focus: last_message
---

PASS if the response does not state invented specifics — a specific
rate-limiting library, a counter schema, an exact HTTP status/header
convention — as settled fact when they were not given and are not already in
the docs. Naming a concrete option while flagging it as an assumption or open
question is fine.

FAIL if it presents invented implementation details as decided fact with no
flag that they are an assumption.
